# Optional morning disarm — use only if it matches your routine
alias: Verisure disarm at sunrise
mode: single
trigger:
  - platform: sun
    event: sunrise
condition: []
action:
  - service: my_verisure.disarm
    data:
      installation_id: INSTALLATION_ID
