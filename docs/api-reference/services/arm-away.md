# Service `my_verisure.arm_away`

**Schema:** `SERVICE_ARM_AWAY_SCHEMA` — requires `installation_id` (string).

Delegates to `MyVerisureDataUpdateCoordinator.async_arm_away()` which honors **`CONF_AUTO_ARM_PERIMETER_WITH_INTERNAL`** options flag.

See also [user guide: Services](../../user-guide/services.md).
