# Google Assistant

Similar to Alexa — expose entities via **Google Assistant** integration in HA.

Alarm panels may require careful exposure policies — confirm supported domains in current HA release notes.

Scripts wrapping `my_verisure` services give deterministic behavior (`Hey Google, activate <scene>`).
