# Use cases

Located in [`use_cases/`](../../custom_components/my_verisure/core/use_cases/).

Examples:

- `AuthUseCase` — login/OTP/session continuity  
- `InstallationUseCase` — list installations / pull services  
- `AlarmUseCase` — status + arm/disarm operations  
- Camera-specific use cases — refresh & dummy directory scaffolding  

Injected via dependency injector ([dependency injection](../developer-guide/dependency-injection.md)).
